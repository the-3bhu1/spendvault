# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files (secrets like RapidAPI keys) — keep the committed template only
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
android/key.properties

# Keystores, builds, and temporary patches
*.jks
*.keystore
# Default mount point for the release keystore (see docs/RELEASE.md). Never commit its contents:
# losing control of that key is worse than losing the key, and losing the key is already bad.
.keys/
*.apk
*.patch
patch_transactions.cjs

# Local tooling config (Claude Code)
.claude/*
!.claude/commands/

# Exported user data backups — never commit personal financial data
spendvault_backup_*.json
